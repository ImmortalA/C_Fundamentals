#include <stdio.h>
#include <math.h>

//break;
//continue;

//Nhap so hang, so cot, in ra hinh chu nhat danh so tang dan

int main()
{
    int i = 0;
    int n = 10;

    for (i = 0; i < n; i++)
    {
        printf("i = %d\n", i);
    }
    printf("After for loop, i = %d", i);

    return 0;
}