#include <stdio.h>

int main()
{
    char character = 'd';
    character++;

    // printf("Nhap 1 ky tu:\n");
    // fflush(stdin);
    // scanf("%c", &character);

    // printf("ASCII = %d\n", character);
    printf("%c", character);

    return 0;
}